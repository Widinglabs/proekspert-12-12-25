# Execute the following plan

Follow the `Steps` to execute the `Plan` then `Report` the work you have done.

## Steps

- Read the plan
- Think hard about it
- Create detailed todos then execute the plan.

## Plan

$ARGUMENTS

## Report

- Summarize the work you've just done in a concise bullet point list.
- Report the files and total lines changed with `git diff --stat`
